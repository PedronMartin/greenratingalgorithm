1) per aggiornare il logo o altre risorse statiche del progetto bisogna ricompilare i file qsr e py relativi (vedi documentazione nei link salvati);
2) query alberi e edifici overpass turbo:
        [out:json][timeout:25];
        (
          // Estrai gli edifici nell'area della mappa
          node["building"]({{bbox}});
          way["building"]({{bbox}});
          relation["building"]({{bbox}});

          // Estrai gli alberi nell'area della mappa
          node["natural"="tree"]({{bbox}});
        );
        out body;
        >;
        out skel qt;

3)
